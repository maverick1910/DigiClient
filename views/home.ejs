<%= console.log(blog) %>
<% for(var log=0;log<(blog.length) ;log++){%>
<h1>
    <%= blog[log].title %></h1>
    <h1>
       <%= blog[log].content %></h1>
       
    <% } %>        

    <!-- <% for(var log=0;log<(test.length) ;log++){%>
        <h1>
            <%= test[log].title %></h1>
            <h1>
               <%= test[log].content %></h1>
               
               <img src="data:test[log]/<%=test[log].img.contentType%>;base64, 
               <%=test[log].img.data.toString('base64')%>"> 
            <% } %>     -->

   